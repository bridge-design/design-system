import { Meta, Canvas, Story, ArgsTable } from "@storybook/blocks";
import CtaLink from ".";

<Meta title="Components/CtaLink" />

## CtaLink

Call-to-action link with decorative arrow afterwards

<Canvas>
  <Story
    name="Default"
    args={{ label: "Check tickets" }}
    parameters={{ design: { disable: true } }}
  >
    {({ label }) => <CtaLink to="https://bridge-the-gap.dev">{label}</CtaLink>}
  </Story>
  <Story
    name="Arrow back"
    args={{ label: "Check tickets" }}
    parameters={{ design: { disable: true } }}
  >
    {({ label }) => (
      <CtaLink to="https://bridge-the-gap.dev" arrow="start">
        {label}
      </CtaLink>
    )}
  </Story>
  <Story
    name="Underline"
    args={{ label: "Check tickets", underline: true }}
    parameters={{ design: { disable: true } }}
  >
    {({ label, ...props }) => (
      <CtaLink to="https://bridge-the-gap.dev" {...props}>
        {label}
      </CtaLink>
    )}
  </Story>
</Canvas>

<ArgsTable of={CtaLink} />
