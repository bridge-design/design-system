import { Meta, Canvas, Story } from "@storybook/addon-docs";
import { ArgsTable } from '@storybook/blocks';

import Card from ".";
import Text from "../Text";

<Meta title="Components/Card" />

## Card

Card/Teaser/Preview. Displays content intro and link to the page with full
content.

export const argsDefault = {
  heading: "5 & 6 May – 10-14 CEST",
  link: { text: "Check tickets", href: "https://bridge-the-gap.dev" },
};
export const argsInGrid = {
  cards: [
    {
      heading: "5 & 6 May – 10-14 CEST",
      link: { text: "Check tickets", href: "https://bridge-the-gap.dev" },
    },
    {
      heading: "Sep ? – 10-14 CEST",
      link: { text: "Get early-bird tickets", href: "https://bridge-the-gap.dev" },
    },
    {
      heading: "Need a custom workshop for your team",
      link: { text: "Let's talk", href: "https://bridge-the-gap.dev" },
    },
  ],
};

<Canvas>
  <Story name="Default" args={argsDefault}>
    {(args) => (
      <Card {...args}>
        <div className="mb-6">
          <Text variant="smBolder" className="mb-3">
            Available tickets:
          </Text>
          <p>Designers: SOLD OUT</p>
          <p>Developers: 1</p>
        </div>
      </Card>
    )}
  </Story>
  <Story
    name="In grid"
    args={argsInGrid}
  >
    {(args) => (
      <div className="grid grid-cols-3 gap-10">
        {args.cards.map((card, i) => (
          <Card {...card} key={card.heading + i}>
            {i !== 2 && (
              <div className="mb-6">
                <Text variant="smBolder" className="mb-3">
                  Available tickets:
                </Text>
                <p>Designers: SOLD OUT</p>
                <p>Developers: 1</p>
              </div>
            )}
          </Card>
        ))}
      </div>
    )}
  </Story>
</Canvas>

<ArgsTable of={Card} />
