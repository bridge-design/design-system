import { Meta, Canvas, Story } from "@storybook/addon-docs";
import { ArgsTable } from '@storybook/blocks';
import theme, { getColors } from "../../utils/getTheme";
import Text from ".";

export const variants = Object.keys(theme.fontSize);
export const colors = getColors();
export const placeholder = "The quick brown fox jumped over the lazy dog.";

<Meta title="Components/Text" />

## Text

Any text on the page

<Canvas>
  <Story name="Default" parameters={{ design: { disable: true } }}>
    <div className="flex flex-col">
      {variants.map((variant) => (
        <Text key={variant} variant={variant}>
          {placeholder}
        </Text>
      ))}
    </div>
  </Story>
  <Story
    name="With As Prop"
    args={{ variant: "base", as: "i" }}
    argTypes={{
      variant: { options: variants, control: { type: "select" } },
    }}
    parameters={{ design: { disable: true } }}
  >
    {(args) => <Text {...args}>{placeholder}</Text>}
  </Story>
  <Story
    name="Responsive"
    args={{ variant: "6Xl" }}
    argTypes={{
      variant: { options: variants, control: { type: "select" } },
      md: { options: variants, control: { type: "select" } },
      lg: { options: variants, control: { type: "select" } },
      xl: { options: variants, control: { type: "select" } },
    }}
    parameters={{ design: { disable: true } }}
  >
    {({ md, lg, xl, ...args }) => (
      <Text
        variant="3Xl"
        as="span"
        responsive={{ md: md, xl: xl, lg: lg }}
        {...args}
      >
        {"Figma and React"}
      </Text>
    )}
  </Story>
</Canvas>

<ArgsTable of={Text} />
