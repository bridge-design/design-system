import { Meta, Story, Canvas, ArgsTable } from "@storybook/blocks";
import Button from ".";
import { ChevronRight } from "@carbon/icons-react";

<Meta title="Components/Button" />

## Buttons

Button component provides interfaces for all types of the buttons. Different types of the
buttons are coded as different components and imported here.

<Canvas>
  <Story
    name="Default"
    args={{ label: "New Button", size: "small" }}
    argTypes={{
      size: { options: ["small", "big"], control: { type: "select" } },
    }}
  >
    {(args) => <Button {...args} />}
  </Story>
  <Story name="Small" args={{ label: "New Button" }} parameters={{ design: { disable: true } }}>
    {(args) => <Button {...args} size="small" />}
  </Story>
  <Story name="Big" args={{ label: "New Button" }} parameters={{ design: { disable: true } }}>
    {(args) => <Button {...args} size="big" />}
  </Story>
  <Story
    name="With Icon"
    args={{ size: "small" }}
    argTypes={{
      size: { options: ["small", "big"], control: { type: "select" } },
    }}
    parameters={{ design: { disable: true } }}
  >
    {(args) => (
      <Button {...args}>
        Send
        <ChevronRight size={24} className="ml-5" />
      </Button>
    )}
  </Story>
  <Story
    name="As Link"
    args={{ label: "New Button", href: "https://www.varya.me" }}
    argTypes={{
      size: { options: ["small", "big"], control: { type: "select" } },
    }}
    parameters={{ design: { disable: true } }}
  >
    {(args) => (
      <Button {...args}>
        Send
        <ChevronRight size={24} className="ml-5" />
      </Button>
    )}
  </Story>
</Canvas>

## Props

<ArgsTable of={Button} />
