import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";

import Icon from ".";
import * as CarbonIcons from "@carbon/icons-react";

<Meta title="Components/Icon" />

## Icon

Wrapper component around Carbon Design Icons. Expects `name` prop with the name of icon.

Full library of icons can be found [here](https://www.carbondesignsystem.com/guidelines/icons/library).
Available icon sizes: **16px**, **20px**, **24px**, **32px**.

These icons we currently use:

export const IconsBridge = [
  "Education32",
  "TableOfContents32",
  "Delivery32",
  "Idea32",
  "EventsAlt32",
  "Forum32",
  "Group32",
  "ChartMarimekko32",
];

<Canvas>
  <Story name="Default" parameters={{ design: { disable: true } }}>
    {IconsBridge.map((icon) => (
      <div className="inline-flex m-2" key={icon}>
        <Icon name={icon} />
      </div>
    ))}
  </Story>
  <Story name="Black&White" parameters={{ design: { disable: true } }}>
    {IconsBridge.map((icon) => (
      <div className="inline-flex m-2" key={icon}>
        <Icon name={icon} />
      </div>
    ))}
  </Story>
  <Story name="Grayed" parameters={{ design: { disable: true } }}>
  <div className="bg-gray-900 p-10">
    {IconsBridge.map((icon) => (
      <div className="inline-flex m-2" key={icon}>
        <Icon name={icon} className="bg-dark-background-800 text-gray-50"/>
      </div>
    ))}
    </div>
  </Story>
</Canvas>

<ArgsTable of={Icon} />
